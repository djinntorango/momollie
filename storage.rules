rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {

    // Product images: anyone can read, only authenticated users can upload
    match /products/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
